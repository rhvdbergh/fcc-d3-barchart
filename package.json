{
  "name": "barchart",
  "version": "1.0.0",
  "description": "freeCodeCamp data visualization project 1",
  "main": "index.js",
  "scripts": {
    "dev": "parcel src/index.html",
    "predeploy": "rm -rf dist && parcel build src/index.html --public-url /fcc-fel-pomodoro/",
    "deploy": "gh-pages -d dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ronald van der Bergh",
  "license": "MIT",
  "devDependencies": {
    "gh-pages": "^2.2.0",
    "parcel": "^1.12.4"
  },
  "dependencies": {
    "d3": "^7.6.1"
  }
}
